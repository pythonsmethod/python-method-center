> **[ARCHIVED — PRE-LEGAL-CLEANUP ARCHITECTURE]**
> Этот документ отражает старую архитектурную концепцию системы до проведения LEGAL-CLEANUP-1/2/3.
> Не использовать как source-of-truth. Актуальная архитектура: `docs/system_identity_v2.md`

---

# PLAN — Python Method Center

## Текущая фаза: MVP до оплаты

После завершения этой фазы — запуск с реальными клиентами.
Только после запуска — начинаем строить фазу «после оплаты».

---

## Определение готовности (MVP)

Бот принимает реального онкопациента и проводит его от первого сообщения
до подтверждённой оплаты на Stripe:

- с пониманием ситуации (профилактика / тяжёлый / продление / попробовать)
- с рекомендацией подходящего тарифа
- с показом оферты до оплаты
- с двумя кнопками согласия («Я ознакомился, готов» / «Вопрос перед оплатой»)
- с надёжным логом факта согласия (timestamp + user_id + версия оферты)
- без блокировки сервера при оплате
- с уведомлением Карена и подтверждением клиенту после оплаты
- с эскалацией к Анне при «вопросе перед оплатой» и в кризисах

Если эти восемь пунктов работают на реальном тестовом клиенте — фаза 1 готова.

---

## Критические находки аудита (Шаг 1)

Главная картина: **вся до-оплатная логика сейчас реализована как инструкции
внутри LLM-промптов**, а не как детерминированный код. Это значит:

- 🔴 **Две кнопки согласия не реализованы как Telegram inline_keyboard** —
  только как текст, который LLM просят нарисовать. Юридически клика нет —
  доказать факт согласия невозможно.
- 🔴 **Факт согласия нигде не сохраняется** (нет timestamp + user_id +
  версия оферты). При chargeback компания не сможет защититься —
  оферта §6 требует «digital confirmation of offer acceptance».
- 🔴 **Stripe-ссылка отправляется через LLM**, а не через код:
  функция `get_payment_link()` в `agents.py:1452` определена, но **ни разу
  не вызывается**. Модель может не отправить ссылку, отправить не тот тариф,
  или галлюцинировать URL.
- 🔴 **Запрет на упоминание капсул/формулы — только в промптах**.
  `response_validator.py` НЕ блокирует слова «капсулы», «формула»,
  «Pythons Elixir», «дозировка». Если модель проигнорирует промпт —
  нарушение уйдёт пациенту.
- 🟡 Понимание 4 ситуаций (профилактика/тяжёлый/продление/попробовать)
  не реализовано — есть только 3 категории в Gabriel.
- 🟡 Маппинг ситуация → тариф отсутствует — Lucky показывает оба тарифа.
- 🟡 Кнопка «вопрос перед оплатой» не существует → нет маршрута эскалации
  к Анне для этого сценария.
- 🟡 Эскалация к Карену для health-вопросов работает только после
  подтверждения summary; прямого роутинга health-вопрос → Карен нет.

Что уже хорошо работает:
- ✅ Кризисная детекция (суицид) → Анне (`agents.py:1376`, `ANNA_CHAT_ID`)
- ✅ Оферта v2 PDF лежит и раздаётся через `/documents/oferta` (`main.py:622`)
- ✅ Все агенты (Sarah, Sophia, Hannah, Maya, Lucky, Gabriel) реализованы
  как промпты + селектор (`agent_selector.py:37-48`)
- ✅ ANNA_CHAT_ID и KAREN_CHAT_ID настроены, инфраструктура эскалации есть

---

## Что СОЗНАТЕЛЬНО отложено в фазу 2 (после оплаты)

Не трогаем до запуска фазы 1, даже если очень хочется.

- Sarah, Sophia, Hannah, Maya — специализированные агенты
- Сбор анализов и медицинских данных (analysis_module — переработка)
- Долгосрочная память клиента между сессиями
- Напоминания о продлении (7д, 3д, день, +7д)
- Тёплое возвращение «Карен спрашивал, как вы»
- Логика «стабилизация → восстановление» для тяжёлых случаев
- Разрезка agents.py на модули
- Закрытый канал PROFESSOR PYTHON (выдача доступа)

---

## Шаги до MVP

### Шаг 1. Аудит до-оплатной зоны
**Статус:** ✅ готово
**Фактическое время:** ~5 минут (делегировано sub-агенту)

Результат — критические находки в секции выше, конкретные задачи — в Шагах 3a-4d ниже.

### Шаг 2. Фикс блокировки сервера при оплате
**Статус:** ✅ готово
**Фактическое время:** ~30 минут

Сделано:
- `agents.py`: `import requests` → `import httpx`; два вызова `requests.post(...)`
  → `httpx.post(..., timeout=10.0)` в `send_karen_paid_notification`
  и `send_payment_thanks`. Добавлен таймаут 10с (раньше его не было —
  при зависании Telegram запрос мог висеть бесконечно).
- `main.py`: `on_payment_confirmed(...)` в `stripe_webhook` обёрнут в
  `await asyncio.to_thread(...)`. Теперь event loop не блокируется на
  время отправки уведомлений в Telegram при поступлении оплаты.

Регрессий нет: 16 fail / 56 pass до правки = 16 fail / 56 pass после.
Падающие тесты — pre-existing, в cognitive-модулях, к фазе 1 не относятся.

### Шаг 2.5. Миграция transport: SendPulse → прямой Telegram Bot API
**Статус:** ✅ готово и подтверждено вживую
**Фактическое время:** ~1 час в коде + 5 минут активации (вместо оценённых 4-6 часов)

**Что выяснилось:** миграция была начата кем-то ранее, и в коде уже
готово 95%:
- `extract_event_telegram` (`main.py:241`) — парсит нативный Telegram update.
- `/telegram/webhook` (`main.py:437`) — endpoint для приёма от Telegram.
- `/telegram/set-webhook` (`main.py:483`) — utility для регистрации.
- `send_message` (`main.py:152`) — ветка `if contact_id.startswith("tg_")`
  → `_send_message_telegram` (прямой Telegram sendMessage).
- `download_telegram_file` в `image_pipeline.py:307` — fallback на прямой
  `getFile` когда нет SendPulse proxy URL.

**Что доделано (этот коммит):**
- `send_document` (`main.py:184`) теперь тоже знает про `tg_` префикс
  и шлёт через `_send_document_telegram` — новый helper, использует
  Telegram `sendDocument` с публичным URL документа. Smoke-test
  подтвердил правильную маршрутизацию обеих веток.

**Что осталось — действия Анны (production change):**

1. **Проверить SendPulse panel.** Если в SendPulse настроены сценарии/
   автоматизации/рассылки для этого бота — они перестанут работать после
   активации webhook (Telegram отдаёт updates либо webhook'у, либо
   polling — не обоим). Решить: оставить SendPulse как secondary tool
   для других задач, или удалить интеграцию полностью.
2. **Активировать webhook.** Открыть в браузере:
   `https://<твой-railway-url>/telegram/set-webhook` — endpoint один раз
   зарегистрирует наш сервер в Telegram. С этого момента сообщения от
   пациентов будут идти к нам напрямую.
3. **Тестирование.** Написать боту тестовое сообщение, убедиться:
   - сообщение приходит на сервер (в логах `[TG_DIRECT]` события)
   - бот отвечает
   - оферта (если показывается) приходит документом
   - вложение от пациента (картинка/файл) обрабатывается

**План отката (на одну команду):**
Открыть в браузере (с заменой токена):
`https://api.telegram.org/bot<TOKEN>/setWebhook?url=`
→ webhook удалится, SendPulse polling возобновится.

После подтверждения стабильности — отказаться от SendPulse-подписки
и удалить sendpulse-код из `main.py` и `image_pipeline.py` отдельным
коммитом (это уже чистая cleanup-работа, не блокирует Шаг 3a).

**Подтверждение вживую:** Анна написала боту через личный Telegram,
сообщение пришло на сервер через `/telegram/webhook`, бот ответил
корректно и в контексте прошлой переписки (сессия по `tg_<chat_id>`
сохранилась с предыдущих тестов через тот же endpoint). Telegram
вернул `{"ok":true,"description":"Webhook is already set"}` — значит
webhook на этот URL уже был установлен ранее; команда подтвердила
текущую регистрацию.

Известный долг (после фазы 1):
- Удалить весь SendPulse-код из `main.py` (`get_sendpulse_token`,
  ветки SendPulse в `send_message`/`send_document`, env vars
  SENDPULSE_*) и из `image_pipeline.py` (комментарии и fallback пути).
- Отказаться от SendPulse-подписки.

### Шаг 3a. Юридический контур оплаты — настоящие кнопки
**Статус:** ✅ готово (требует live-test от Анны)
**Фактическое время:** ~2 часа

Сделано:

**Новый модуль `consent_log.py`** — PostgreSQL-таблица `pre_payment_consent`
(`contact_id, tariff, offer_version, consented_at TIMESTAMPTZ, source`)
с lazy-init pool и graceful fallback если `DATABASE_URL` не задан
(бот продолжает работать, просто без записи согласия). Закрывает
требование оферты §6 («digital confirmation of offer acceptance»).

**Helpers в `main.py` для Telegram inline-кнопок:**
- `_send_inline_keyboard_telegram` — отправка `sendMessage` с `reply_markup`
- `_answer_callback_query_telegram` — `answerCallbackQuery` (убирает спиннер)
- `_edit_remove_keyboard_telegram` — `editMessageReplyMarkup` (удаляет
  кнопки после клика, защита от двойного нажатия)

**Бизнес-логика согласия в `main.py`:**
- `_parse_oferta_marker` — парсит `[SEND_OFERTA]` или `[SEND_OFERTA:N]`
- `_build_consent_keyboard` — 2 кнопки если тариф известен,
  3 кнопки (по одной на каждый тариф + «вопрос») если нет
- `_send_oferta_and_consent_buttons` — оферта PDF + клавиатура
- `_handle_consent_callback` — обработчик `consent_yes:N` (логирует
  факт + детерминированно посылает Stripe-ссылку через
  `get_payment_link()`, не через LLM) и `consent_question`
  (форвардит контекст диалога Анне на `ANNA_CHAT_ID`)
- `_forward_to_anna` — helper для админ-уведомлений

**Расширен `/telegram/webhook`:**
- Новая ветка 0 — обработка `callback_query` перед обычным `message`
- После reply от LLM — парсинг и обработка `[SEND_OFERTA]` маркера
  в обеих ветках (text и attachment-only)

**Maya промпт обновлён** (`agents.py`): убрана инструкция «нарисуй
две кнопки текстом», явно требует ставить `[SEND_OFERTA:1]` или
`[SEND_OFERTA:2]` (с указанием тарифа), запрещено вставлять
Stripe-ссылку — это делает код.

**Sanity-test:** парсер маркера 5/5, keyboard builder выдаёт
правильные 2- и 3-кнопочные клавиатуры для всех tariff_hint.

**Что закрыто из критических находок аудита:**
- ✅ Настоящие Telegram inline-кнопки (раньше только текст в промпте)
- ✅ Лог согласия в БД с timestamp + tariff + offer_version
- ✅ Stripe-ссылка отправляется из кода через `get_payment_link()`
  (раньше через LLM, без гарантий)
- ✅ Кнопка «вопрос перед оплатой» работает: контекст диалога
  форвардится Анне на ANNA_CHAT_ID

**Live-test (от Анны):** написать боту до момента выбора тарифа,
получить оферту + кнопки, кликнуть «готов(а)» — должна прийти
Stripe-ссылка. Кликнуть «вопрос» — Анна получает уведомление.

Без этого фаза 1 не может быть запущена для реальных людей.

- Реализовать настоящий Telegram `inline_keyboard` с двумя кнопками:
  «Я ознакомился с условиями и готов к участию» (`callback_data=consent_yes`)
  и «У меня есть вопрос перед оплатой» (`callback_data=consent_question`).
- Добавить обработчик `callback_query` в `/telegram/webhook` в `main.py`.
- Создать таблицу `pre_payment_consent` (user_id, ts UTC, offer_version,
  tariff_chosen) и писать запись при клике `consent_yes`.
- После клика `consent_yes` — детерминированно (из кода, не через LLM)
  отправлять Stripe-ссылку через существующую `get_payment_link(contact_id,
  tariff_number)` из `agents.py:1452`.
- После клика `consent_question` — детерминированно форвардить контекст
  диалога Анне через `escalation_manager` (ANNA_CHAT_ID уже есть).

### Шаг 3b. Блок-лист «капсулы/формула» в response_validator (КРИТИЧНО)
**Статус:** ✅ готово, ✅ подключено в проде, ✅ расширено по корректировке Анны
**Фактическое время:** ~55 минут изначально + 30 минут на подключение и расширение

Сделано — двойная защита:

**(2b) Исходящий фильтр** — бот не может сам начать говорить про капсулы.
`_CAPSULE_FORMULA_PATTERNS` (regex): `капсул`, `формул`, `эликсир`, `elixir`,
`pythons\s+elixir`, `дозиров`, `состав\s+(препарат|капсул|формул|эликсир)`.
При срабатывании — ответ подменяется на fallback из «Голос Анны».

**(2c) Входящий триггер** — гарантированная маршрутизация к Карену когда
клиент сам спросил про капсулы. Если последнее сообщение клиента содержит
`_INCOMING_CAPSULE_PATTERNS` (`капсул`, `формул`, `эликсир`, `elixir`,
`pythons\s+elixir`, `дозиров`, `capsul`, `dosag`), а ответ LLM не содержит
И упоминания Карена (`карен`/`karen`) И сигнала «он сам разберётся»
(`лично`/`сам`/`сама`/`после`/`в работу`/`индивидуально`/`personally`) —
ответ подменяется на буквальный fallback. Это закрывает риск, что модель
проигнорирует прямой вопрос или уведёт тему.

Контекст: по «Голос Анны» Карен бесплатно присылает Pythons Elixir
каждому, кто оплатил программу — это его личная инициатива вне бота
и оферты. Поэтому ответ обязан оставлять у клиента ощущение, что Карен
ответит лично, а не «у нас этого нет».

**Sanity-test 13/13:** все нарушения (исходящие + игнорирование входящих
вопросов) ловятся; критичные edge cases («состав крови», «доставка
договора») не триггерят.

Известный долг (после фазы 1): создать `test_response_validator.py`
с покрытием Check 1-6 — сейчас постоянных тестов на модуль нет.

Опционально (можно сейчас, можно позже): усилить промпты Lucky и Maya
в `agents.py:767` и `agents.py:948` — добавить буквальный эталонный
ответ из «Голос Анны» как пример. Тогда валидатор будет страховкой,
а не основным механизмом ответа.

**Дополнение от 2026-05-24 (live-test Анны):**
Анна заметила, что бот в реальном ответе сказал «составляет
индивидуальный протокол капсул / состав под вашу ситуацию /
дозировки / схема приёма / длительность курса» — то есть выдал
именно ту формулировку, которую запрещает концепция: формула
Карена единая и патентуется, индивидуальный состав не подбирается.

Расследование показало, что `ResponseValidator` существовал, но
**не вызывался** в активном пути `/telegram/webhook → process_message →
send_message`. Он вызывался только из `orchestrator_core.py`, который
включается флагом `USE_NEW_MESSAGE_PIPELINE` (выключен в проде).
Это значит — все ✅ Шага 3b в нашей сессии до этого момента ловили
нарушения в unit-тестах, но в реальных ответах бота не срабатывали.

Исправлено:
- `main.py` импортирует `ResponseValidator`, держит singleton
- Добавлена helper `_validate_outgoing_reply(reply, contact_id)` —
  вызывает валидатор, при `not safe` подменяет на `safe_reply`,
  ошибки самого валидатора не блокируют поток.
- `_validate_outgoing_reply` вызывается в обоих ветках
  `/telegram/webhook` (text + attachment-only) **перед** `send_message`.

Расширены `_CAPSULE_FORMULA_PATTERNS` по корректировке Анны:
- `протокол\s+капсул`
- `индивидуальный\s+состав`
- `состав\s+под\s+(ваш|тво|ситуац|случа)`
- `подбирает\b[^.]{0,30}(состав|протокол|формул|капсул|дозиров|компонент)`
- `схема\s+при[её]ма`

Проверено на реальном тексте из скриншота: ответ полностью
подменяется на fallback из «Голос Анны».

### Шаг 4a. Классификатор 4 ситуаций + маппинг тарифов
**Статус:** ⏳ зависит от 3a
**Оценка:** 2-3 часа

Сейчас Gabriel классифицирует 3 категории, Lucky показывает оба тарифа сразу.
Концепция требует распознавания 4 ситуаций и точечной рекомендации.

- Добавить классификатор ситуации в `agent_selector` или отдельный модуль:
  `prophylaxis` / `severe_case` / `continuation` / `wanna_try`.
- Сохранять ситуацию в session.
- Маппинг: `severe_case → Tariff 2`, `prophylaxis|wanna_try|continuation → Tariff 1`.
- Lucky показывает **один** рекомендованный тариф (по «Голосу Анны»).

### Шаг 4b. Сверка промптов Lucky/Maya с концепцией
**Статус:** ⏳ зависит от 3a
**Оценка:** 1-2 часа

Деваций, найденные в аудите:
- Framing «ангел-хранитель» (`agents.py:767`) — нет в концепции, убрать
  или согласовать.
- Упоминания «дыхание/движение/йога» (`agents.py:804`) — сверить с концепцией;
  если это не из «Голос Анны» — убрать.
- Эталонные ответы из «Голос Анны» (на «А точно работает?», на «4 стадия»)
  встроить буквально в промпт Lucky как примеры.
- Удалить инструкцию «нарисуй две кнопки текстом» — теперь это делает код
  (после 3a).

### Шаг 4c. Расширение кризисной детекции
**Статус:** ⏳ может быть параллельно
**Оценка:** 1 час

`agents.py:1376` сейчас детектит только русские суицидальные слова.
- Добавить английские варианты («suicide», «kill myself», «want to die»…).
- Отдельные кейворды для агрессии/мошенников → тоже к Анне, но с другим тегом.
- Guard на минора без представителя (отсутствует, концепция требует).

### Шаг 4d. Прямая эскалация health-вопросов к Карену
**Статус:** ⏳ может быть параллельно
**Оценка:** 1-2 часа

Сейчас вопросы о здоровье уходят к Карену только после подтверждения
summary. Концепция требует немедленной эскалации при любом медицинском
вопросе («может ли мне помочь ваш метод?» и т.п.).

Добавить классификатор health-вопросов → форвард к Карену через
`escalation_manager` с пометкой «требует ответа реабилитолога».

### Шаг 5. Live-test до оплаты
**Статус:** ⏳ зависит от Шагов 1-4
**Оценка:** 1 час

Пройти весь сценарий как реальный пользователь — на тестовом ключе Stripe.
Зафиксировать баги. Поправить.

### Шаг 6. Запуск
**Статус:** ⏳ финальная точка
**Оценка:** —

Объявить готовность. Принимать реальных людей.

---

## Правила работы (контракт)

1. Не уходим за границу фазы 1 без явного решения.
2. На каждое предложение «давай добавим Х» — вопрос: «это для MVP, или откладываем в фазу 2?»
3. Каждый шаг с оценкой времени; если затягивается в 2-3 раза дольше — стоп, разбор.
4. Файлы концепции читаем точечно, только когда упёрлись в конкретный вопрос.
5. Текущий статус каждого шага — в этом файле, обновляем по ходу.
6. В новой сессии (новый чат, другой день, другая модель) — первым делом читаем этот файл.

---

## Источники истины (что прочитано)

- **Python Method Knowledge Base** — базовая концепция, тон, границы, тарифы, эскалация
- **Голос Анны** — формулировки, эталонные ответы, философия онкопсихологии
- **Python Method Public Offer (EN)** — оферта v1, юридические рамки
- `agents.py` — реализация диалога и Stripe webhook (частично, продолжаем читать)

## Ждёт прочтения по мере необходимости

| Файл | Когда читать |
|---|---|
| Central AI Core v2 Analysis | Возможно полезно сейчас — карта готовности от Анны |
| Biblia_System_v1_3 Sections 23-26 Central AI Core | Фаза 2 |
| Biblia_System_v1_3 Section 23 Context Protocol | Фаза 2 |
| 23_Долгосрочная_память_клиента | Фаза 2 |
| Python_Method_Center_Governance_v1 | По необходимости |
| Python_Method_Role_Distribution_v1_0 | Фаза 2 (когда строим других агентов) |
| Python Method Oferta v2 | На Шаге 3, чтобы показывать актуальную версию |
| Biblia_System_Phase4_Addendum | По необходимости |
| agents.pdf | Не нужно (есть исходный код) |

---

## Известные технические долги (вне фазы 1)

Обнаружено по ходу, не для немедленного фикса:
- 16 unit-тестов падают на чистом репозитории (cognitive-модули:
  `central_cognitive_orchestrator`, `expert_load_balancing_engine`,
  `rehabilitation_route_simulation`). Не связаны с до-оплатной зоной.
  Разобрать после запуска фазы 1.
- Endpoint `/telegram/get-webhook` (`main.py:511`) открыт без авторизации.
  Любой, кто узнает URL сервера, может вызвать. После миграции transport
  закрыть простым shared secret в query-параметре.

---

## Ключевые контакты (из концепции)

- **Карен** — автор метода, @armenianpythonusa, закрытый канал PROFESSOR PYTHON
- **Анна** — координатор, @anna_dubrovenko, психологические эскалации и «вопрос перед оплатой»
- **Pythons & Co Inc** — 1331 Amherst Ave, Apt PH5, Los Angeles, CA 90025

## Тарифы (сматчены с кодом)

| Тариф | Цена | Длительность | `amount_total` в Stripe |
|---|---|---|---|
| Знакомство | $1,113 (1060 + 5%) | 6 недель | `111300` ✅ |
| Полное сопровождение | $4,725 (4500 + 5%) | 21 неделя | `472500` ✅ |
